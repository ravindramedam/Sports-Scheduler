<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>View Reports</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <% if (typeof include === "function") { %> 
                <%- include('header.ejs') %>
            <% } %>

            <b class="text-white bg-blue-600 hover:bg-blue-800 font-medium rounded px-3 py-1">
                <a href="/viewReports">Go Back</a>
            </b>
            <b class="text-white bg-blue-600 hover:bg-blue-800 font-medium rounded px-3 py-1">
                <a href="/signout">Signout</a>
            </b>

            <h2 class="mt-3 text-blue-600"><b>Reports:</b></h2>
            <p>Total number of sessions organized: <b><%= NoOfSess %></b></p>
            <p>From: <b><%= new Date(Date1).toDateString() %>, <%= new Date(Date1).toLocaleTimeString() %></b></p>
            <p>To: <b><%= new Date(Date2).toDateString() %>, <%= new Date(Date2).toLocaleTimeString() %></b></p>

            <h2 class="mt-3 text-blue-600"><b>Number of Sport Sessions:</b></h2>
            <div class="flex-container">
                <% if (reports.length > 0) { %>
                    <% reports.forEach((report, index) => { %>
                        <div class="font-medium rounded px-2 py-1">
                            <%= report.sportName %>: <%= report.count %>
                            <% if (index === 0) { %> 
                                <p class="rounded inline">(Most Popular)</p> 
                            <% } %>
                        </div>
                    <% }) %>
                <% } else { %>
                    <p>No Sport Sessions were organized</p>
                <% } %>
            </div>
        </div>
    </div>
</body>

</html>
