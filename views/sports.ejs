<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sports Scheduler</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        body {
            background: linear-gradient(to right, #1e3c72, #2a5298);
            color: white;
            font-family: 'Open Sans', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
        }

        .sidebar {
            width: 250px;
            background: rgba(11, 184, 214, 0.9);
            padding: 20px;
            height: 100vh;
            position: fixed;
        }

        .sidebar a {
            color: white;
            display: block;
            padding: 10px;
            margin: 10px 0;
            text-decoration: none;
            border-radius: 5px;
            transition: 0.3s;
        }

        .sidebar a:hover {
            background: #006bdd;
        }

        .content {
            margin-left: 270px;
            padding: 50px;
            width: calc(100% - 270px);
        }

        .container {
            max-width: 1000px;
            margin: auto;
            background-color: rgba(0, 0, 0, 0.8);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.5);
        }

        .header-container h1 {
            background-color: #006bdd;
            padding: 15px;
            border-radius: 8px;
            color: white;
            text-shadow: 2px 2px 6px #000000;
            text-align: center;
        }

        .btn-custom {
            display: block;
            width: 100%;
            padding: 12px;
            margin-top: 10px;
            font-size: 1rem;
            font-weight: bold;
            border: none;
            border-radius: 8px;
            text-align: center;
            text-decoration: none;
            color: black;
            background: #00ffff;
            transition: 0.3s;
        }

        .btn-custom:hover {
            background: #00c0c0;
            color: black;
        }
    </style>
</head>

<body>
    <div class="sidebar">
        <h3 class="text-white text-center">Dashboard</h3>
        <a href="/">Home</a>
        <a href="/createSport">Create Sport</a>
        <a href="/viewReports">View Reports</a>
        <a href="/signout">Sign Out</a>
    </div>
    


    <div class="content">
        <div class="container">
            <div class="header-container">
                <h1>SPORTS SCHEDULER</h1>
            </div>

            <% if (role === 'admin') { %>
                <h2 class="text-info">Administrative Functions:</h2>
                <a href="createSport" class="btn-custom">Create a Sport</a>
                <a href="viewReports" class="btn-custom">View Reports</a>
               
            <% } %>

            <h3 class="text-warning mt-4">Upcoming Sessions:</h3>
            <%- include('homeSessions.ejs') %>

            <h3 class="text-success mt-4">Available Sports</h3>
            <div>
                <% if (allSports.length != 0) { %>
                    <% for (var i = 0; i < allSports.length; i++) { %>
                        <a href="sport/<%= allSports[i].id %>" class="btn-custom">
                            <%= allSports[i].title %>
                        </a>
                    <% } %>
                <% } else { %>
                    <p>No available sports at the moment.</p>
                <% } %>
            </div>
        </div>
    </div>
</body>

</html>
